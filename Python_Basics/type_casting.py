# int(): Converts text to an Integer (whole number).
# float(): Converts text to a Float (decimal number).


# ----------------TASK---------------------
# The Goal: Write a program that:
# Asks the user for one number.
# Asks the user for a second number.
# Calculates and prints the sum (addition) of those two numbers.

first_number = int(input("Enter the first number: "))
second_number = int(input("Enter the second number: "))
print(first_number + second_number)
