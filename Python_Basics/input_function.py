# input() function is used to intereact with the user and take input Always returns string
# It pauses the program and waits for the user to type something.
# variable_name = input("Prompt text goes here: ")
# prompt is what the user sees
# The Pause: Python stops everything and waits for the user to type and hit Enter.
# The Capture: Whatever the user typed is captured and stored in variable_namee


# # ----------TASK---------
# The Goal: create a program that acts like a simplified greeter.

# Ask the user for their first name.

# Ask the user for their favorite color.

# Print a message that uses both pieces of information (e.g., "Hello Alex, I see you like Blue").

first_name = input("Enter your first name: ")
fav_color = input("What is your fave color?: ")
print(f'Hello {first_name} I see you like {fav_color}')
# Hello haraf I see you like yellow
